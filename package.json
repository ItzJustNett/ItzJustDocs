{
  "devDependencies": {
    "vitepress": "^1.6.3"
  },
  "scripts": {
    "build": "vitepress build",
    "deploy": "vitepress build && git add dist && git commit -m 'deploy' && git push origin main && git subtree push --prefix dist origin gh-pages",
    "docs:dev": "vitepress dev config",
    "docs:build": "vitepress build config",
    "docs:preview": "vitepress preview config"
  }
}